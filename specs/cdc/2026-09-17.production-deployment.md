# Publication du CDC — 17 septembre 2026

## Version publiée

- Dépôt : `grizou-Sport/Momentum`.
- PR CDC principale : [#4](https://github.com/grizou-Sport/Momentum/pull/4), fusionnée dans `main` au commit `16ba0b65e1952bcfa573d61b88f2e8ca3b131f26`.
- Correctif découvert pendant la recette publiée : [#5](https://github.com/grizou-Sport/Momentum/pull/5), fusionné au commit final `00b47d0a11be0cee6df8e31d2f4b1889f8894ff5`.
- CI du correctif : [MOMENTUM quality #44](https://github.com/grizou-Sport/Momentum/actions/runs/35269385857), avec `Build and tests`, `CDC readiness` et le contrôle obligatoire `Source and tests` réussis.
- Construction locale équivalente avant publication : 222 tests réussis et 204 fichiers statiques produits.

## Vercel

- URL publiée : <https://momentum-alpha-rho.vercel.app>.
- Déploiement : `dpl_3mT8pqqkSBismD6cyyf3VY5dYBLq` ([inspection](https://vercel.com/momentum-alpha/momentum-alpha/3mT8pqqkSBismD6cyyf3VY5dYBLq)).
- Cible : `production` ; état : `READY` le `2026-09-17T20:20:22.684Z` ; erreur d’alias : aucune.
- Source Vercel : branche `main`, commit GitHub vérifié `00b47d0a11be0cee6df8e31d2f4b1889f8894ff5`.
- `version.json` servi par l’alias : même commit, environnement `production`, accès compte activé.

## Supabase

- Projet : `njcqcpyiiibudlalnzoa`.
- 36 migrations présentes : 18 historiques et 18 migrations CDC, jusqu’à `cdc_client_write_contract`.
- Fonctions actives : `account-deletion` v3, `storage-cleanup` v2 et `file-ingest` v3.
- Le correctif v3 conserve les origines configurables et garantit l’origine publiée dans `account-deletion` et `file-ingest`.
- Les deux pré-vols CORS depuis `https://momentum-alpha-rho.vercel.app` répondent `204` avec cette origine exacte.
- Sans authentification, `file-ingest` répond `401`, `storage-cleanup` répond `401` et `account-deletion` atteint la validation de requête sans exécuter de suppression.
- Le cron `momentum-storage-cleanup` est actif. Au relevé final, 25 exécutions avaient réussi ; la dernière s’est terminée le `2026-09-17T20:21:00.018212Z`.
- Les deux participations historiques acceptées possèdent une révision de planning confirmée ; aucun reliquat de backfill.

## Parcours publié LIV-06

Le contrôle a été réalisé dans un navigateur neuf, sans compte ni écriture de données :

1. `/` redirige un visiteur anonyme vers `/login.html?returnTo=index.html` et affiche la connexion.
2. « Découvrir MOMENTUM » ouvre `/discover.html`, affiche l’exemple explicitement fictif et en lecture seule.
3. « Commencer » ouvre `/login.html?mode=signup`, sélectionne l’onglet de création de compte et affiche la confirmation du mot de passe.
4. Les contrôles HTTP, CORS, fonctions, migrations et cron concordent avec le commit et l’URL publiés.

LIV-06 est donc réussi. Les cinq essais qualitatifs LIV-05 restent explicitement reportés par le propriétaire ; ils ne sont ni simulés ni déclarés réussis. Le statut global demeure `ready-for-release`, tandis que la publication technique et le chapitre 32 sont vérifiés.

## Avis Supabase

Le relevé après migration ne contient pas d’avis de niveau erreur. Les tables privées sans politique restent volontairement inaccessibles au navigateur malgré RLS ; les avis de performance et la protection Auth contre les mots de passe compromis sont consignés comme améliorations séparées, sans élargir ce correctif de publication.
